FROM python:3.12-slim

RUN pip install uv

WORKDIR /app
COPY pyproject.toml .
COPY src/ src/

RUN uv sync --no-dev

CMD ["uv", "run", "uvicorn", "care_passport.api:app", "--host", "0.0.0.0", "--port", "8000"]
